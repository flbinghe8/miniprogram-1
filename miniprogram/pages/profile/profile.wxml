<!-- pages/profile/profile.wxml - 完整修复版 -->
<view wx:if="{{showLoginButton}}" class="login-section">
  <view class="login-card">
    <text class="login-title">未登录</text>
    <text class="login-desc">{{userProfile.packageInfo}}</text>
    <button class="login-button" bindtap="handleLogin">微信一键登录</button>
  </view>
</view>

<view class="container">
  <!-- 头部：会员状态与额度 -->
  <view class="profile-header">
    <view class="user-info">
      <!-- 点击头像弹出功能菜单 -->
      <view class="avatar-placeholder" catchtap="onCreditCardTap">👤</view>
      <view class="nickname">{{ userProfile.nickname }}</view>
    </view>
    
    <!-- 额度信息卡片 -->
    <view class="membership-card credit-card">
      <view class="status-section">
        <view class="card-header">
          <text class="status-title card-title">当前额度</text>
        </view>
        <text class="status-value credit-count">{{ userProfile.credits }}</text>
        <text class="package-info" wx:if="{{userProfile.packageInfo}}">{{userProfile.packageInfo}}</text>
      </view>
      
      <!-- 升级按钮 -->
      <button class="upgrade-btn" catchtap="goToMembership">
        升级会员 / 查看套餐
      </button>
    </view>
  </view>

  <!-- 菜单列表 -->
  <view class="menu-list">
    <view class="menu-item" bindtap="goToHistory">
      <text class="menu-text">历史生成记录</text>
      <text class="menu-arrow">></text>
    </view>
    
    <view class="menu-item" bindtap="goToGuide">
      <text class="menu-text">使用指引 / 添加到桌面</text>
      <text class="menu-arrow">></text>
    </view>
    
    <view class="menu-item" bindtap="contactCustomerService">
      <text class="menu-text">联系客服</text>
      <text class="menu-arrow">></text>
    </view>
    
    <view class="menu-item" bindtap="logout">
      <text class="menu-text">退出登录</text>
      <text class="menu-arrow">></text>
    </view>
  </view>
  
  <view class="app-version">Version 1.3.0</view>
</view>

<!-- 额度明细弹窗 -->
<view class="quota-popup" wx:if="{{showQuotaPopup}}">
  <view class="popup-mask" catchtap="onCloseQuotaPopup"></view>
  <view class="popup-content">
    
    <!-- 标题 -->
    <view class="popup-header">
      <text class="popup-title">额度明细</text>
      <text class="popup-close" catchtap="onCloseQuotaPopup">×</text>
    </view>
    
    <!-- 核心额度信息 -->
    <view class="quota-card">
      <view class="quota-main">
        <text class="quota-count">{{quotaData.totalCredits || 0}}</text>
        <text class="quota-unit">次</text>
      </view>
      <text class="quota-desc">剩余总次数</text>
    </view>
    
    <!-- 额度详情 -->
    <view class="quota-details">
      <!-- ✅ 修复：新增统一显示 -->
      <view class="detail-item">
        <text class="detail-label">当前状态</text>
        <text class="detail-value">{{quotaData.displayText}}</text>
      </view>
      <view class="detail-item">
        <text class="detail-label">试用剩余</text>
        <text class="detail-value">{{quotaData.remainingTrials || 0}}次</text>
      </view>
      <view class="detail-item">
        <text class="detail-label">付费剩余</text>
        <text class="detail-value">{{quotaData.paidCredits || 0}}次</text>
      </view>
      <view class="detail-item">
        <text class="detail-label">已用次数</text>
        <text class="detail-value">{{quotaData.trialUsed || 0}}次</text>
      </view>
      <view class="detail-item">
        <text class="detail-label">套餐类型</text>
        <text class="detail-value">{{quotaData.packageType || '试用套餐'}}</text>
      </view>
      <view class="detail-item" wx:if="{{quotaData.isMember}}">
        <text class="detail-label">会员状态</text>
        <text class="detail-value member">有效</text>
      </view>
    </view>
    
    <!-- 空状态 -->
    <view class="empty-records">
      <text>暂无使用记录</text>
    </view>
    
    <!-- 底部按钮 -->
    <view class="popup-footer">
      <button class="btn-confirm" bindtap="onCloseQuotaPopup">确定</button>
    </view>
    
  </view>
</view>